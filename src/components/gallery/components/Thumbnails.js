import { memo } from "react";
import styled from "styled-components";

const Thumbnails = () => {
  return <StyledThumbnails></StyledThumbnails>;
};

const StyledThumbnails = styled.ul``;

export default memo(Thumbnails);
